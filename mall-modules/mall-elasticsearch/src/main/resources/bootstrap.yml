server:
  port: 9007
# Spring
spring:
  application:
    # 应用名称
    name: mall-elasticsearch
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
